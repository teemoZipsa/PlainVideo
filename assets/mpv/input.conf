# PlainVideo input ownership

# The native Win32 shell owns pointer, keyboard, drag, fullscreen, and menu
# behavior. libmpv receives only explicit Rust commands and script bindings.
# Keeping this file intentionally empty prevents a second input layer from
# reintroducing stock mpv gestures such as Alt+drag window movement.
