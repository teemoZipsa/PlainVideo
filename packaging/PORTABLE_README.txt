PlainVideo Slice 0B developer portable proof

Run:
  plainvideo.exe "C:\path\to\video.mkv"

You can also open a file from the right-click menu or drag a media file onto
the playback surface. Right-click also reveals window move and close actions;
Alt+drag moves the borderless window directly.

This directory is a local architecture proof, not a release artifact. The
pinned libmpv build is conservatively treated as GPL-2.0-or-later, and its
complete redistribution/source/notice review is not finished. Do not publish
or redistribute this directory.
